{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 62013",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 62013",
    "test:rules": "esbuild scripts/check-rules.ts --bundle --platform=node --format=esm --outfile=scripts/.check-rules.mjs --log-level=warning && node scripts/.check-rules.mjs && rm scripts/.check-rules.mjs",
    "test:store": "esbuild scripts/check-store.ts --bundle --platform=node --format=esm --outfile=scripts/.check-store.mjs --log-level=warning && node scripts/.check-store.mjs && rm scripts/.check-store.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0"
  }
}
